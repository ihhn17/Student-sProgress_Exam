@model StudentsProgress.Web.Data.Entities.UserRating

@{
    ViewData["Title"] = "Edit";
}

<h1>Edit</h1>

<h4>@Model.Student.User.LastName @Model.Student.User.FirstName's rating</h4>
<hr />
<div class="row">
    <div class="col-md-4">
        <form asp-action="Edit">
            <div asp-validation-summary="ModelOnly" class="text-danger"></div>
            <input type="hidden" asp-for="Id" />
            <div class="form-group">
                <label asp-for="SemestrPoints" class="control-label"></label>
                <input id="semestr-points" asp-for="SemestrPoints" class="form-control" />
                <span asp-validation-for="SemestrPoints" class="text-danger"></span>
            </div>
            <div class="form-group">
                <label asp-for="SumPoints" class="control-label"></label>
                <input id="sum-points" asp-for="SumPoints" class="form-control" />
                <span asp-validation-for="SumPoints" class="text-danger"></span>
            </div>
            <div class="form-group">
                <label asp-for="Subject" class="control-label"></label>
                <input id="subject-name" disabled="" asp-for="Subject.Name" class="form-control" />
            </div>
            @Html.HiddenFor(x => x.SubjectId)
            @Html.HiddenFor(x => x.StudentId)
            @Html.HiddenFor(x => x.Student.User.UserName, new { id = "user" })
            <div class="form-group">
                <input id="edit-rating" type="submit" value="Save" class="btn btn-primary" />
            </div>
        </form>
    </div>
</div>

<div>
    <a asp-action="Index">Back to List</a>
</div>

@section Scripts {
    @{await Html.RenderPartialAsync("_ValidationScriptsPartial");}
}
